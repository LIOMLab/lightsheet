"""Custom Qt widgets promoted into panel ``.ui`` files via Qt Designer.

This package exists so the Qt Designer promotion header path
``lightsheet.gui.widgets.field_spec_spinbox`` resolves at runtime. Widgets
here are stock-Qt6 subclasses with no third-party plotting dependency.
"""
